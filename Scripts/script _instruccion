### Paso 1: Crear el archivo del script (.sh)
Este archivo contendrá todos los comandos que definimos antes. Usaremos el shell de bash, que es estándar en Linux y macOS y se puede instalar fácilmente en Windows (con Git Bash, por ejemplo).

Abre tu editor de código (como VS Code, Sublime Text, etc.).

Crea un nuevo archivo y llámalo analizar-repo.sh. Es importante que la extensión sea .sh.

Pega el siguiente código en el archivo:

Bash

#!/bin/bash

# --- Script para Análisis Exhaustivo de Repositorios JS/TS ---

# Colores para una salida más amigable en la consola
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
NC='\033[0m' # Sin color

echo -e "${GREEN}🚀 Iniciando análisis exhaustivo del repositorio...${NC}"

# Nombre de la carpeta para los reportes
REPORT_DIR="analysis-reports"

# Crear la carpeta de reportes si no existe
mkdir -p $REPORT_DIR

# 1. Estructura de archivos
# Verifica si el comando 'tree' está disponible
if command -v tree &> /dev/null
then
    echo -e "${YELLOW}🌳 Generando estructura de archivos...${NC}"
    tree -I "node_modules|dist|build|coverage|${REPORT_DIR}" > "${REPORT_DIR}/project-structure.txt"
else
    echo "Advertencia: El comando 'tree' no está instalado. Omitiendo la generación de estructura de archivos."
fi

# 2. Calidad y complejidad
echo -e "${YELLOW}✅ Generando reporte de calidad con ESLint...${NC}"
npx eslint . --format json --output-file "${REPORT_DIR}/eslint-report.json"

echo -e "${YELLOW}🧠 Calculando complejidad del código...${NC}"
# Asumimos que el código fuente está en 'src'. Cámbialo si es necesario.
npx ts-complexity --format json --input src/ > "${REPORT_DIR}/complexity-report.json"

# 3. Seguridad
echo -e "${YELLOW}🔒 Analizando vulnerabilidades de dependencias con Snyk...${NC}"
npx snyk test --json > "${REPORT_DIR}/snyk-dependencies-report.json"

echo -e "${YELLOW}🔒 Analizando tu código en busca de vulnerabilidades con Snyk...${NC}"
npx snyk code test --json > "${REPORT_DIR}/snyk-code-report.json"

# 4. Dependencias
echo -e "${YELLOW}📦 Generando árbol completo de dependencias...${NC}"
npm ls --json > "${REPORT_DIR}/dependencies-tree.json"

echo -e "${GREEN}✨ ¡Análisis completado! Los reportes están en la carpeta '${REPORT_DIR}'.${NC}"
Guarda el archivo en la raíz de uno de tus proyectos para probarlo.

Haz que el script sea ejecutable. Antes de poder correrlo, necesitas darle permisos de ejecución. Abre una terminal en la raíz de tu proyecto y ejecuta:

Bash

chmod +x analizar-repo.sh
¡Listo! Ya tienes tu script. Para ejecutarlo, simplemente corre el siguiente comando en tu terminal:
./analizar-repo.sh

### Paso 2: Cómo Integrar el Script en tus Proyectos
Aquí tienes dos métodos, del más simple al más profesional y recomendado.

#### Método 1: Copiar el Script en cada Proyecto (El más simple)
La forma más directa es simplemente copiar el archivo analizar-repo.sh a la carpeta raíz de cada proyecto en el que quieras usarlo.

Pros: Rápido y no requiere configuración adicional.

Contras: Si actualizas el script, tendrás que actualizarlo manualmente en todos los proyectos.

#### Método 2: Integrarlo como un Script de NPM (Recomendado)
Esta es la mejor práctica para proyectos de Node.js. Al añadirlo al package.json, cualquier persona que trabaje en el proyecto puede ejecutar el análisis con un comando de npm estándar y familiar.

Asegúrate de que el archivo analizar-repo.sh esté en la raíz de tu proyecto.

Abre tu archivo package.json.

Busca la sección "scripts" y añade una nueva línea. Puedes llamarlo "analyze" o como prefieras.

JSON

{
  "name": "mi-proyecto",
  "version": "1.0.0",
  "scripts": {
    "start": "node index.js",
    "test": "jest",
    "lint": "eslint .",
    "analyze": "bash ./analizar-repo.sh"
  }
}
¡Eso es todo! Ahora, en lugar de recordar el nombre del archivo .sh, puedes simplemente ejecutar:

Bash

npm run analyze
Este comando hará exactamente lo mismo, pero está integrado de forma nativa en el flujo de trabajo de tu proyecto.

### Prerrequisitos: Herramientas Necesarias
Recuerda que este script depende de las herramientas que invoca. Asegúrate de tenerlas instaladas, preferiblemente de forma global para que estén disponibles en cualquier proyecto.

Snyk CLI:
npm install -g snyk

ts-complexity:
npm install -g ts-complexity

Tree:

Windows: Ya viene instalado.

macOS: brew install tree

Debian/Ubuntu: sudo apt-get install tree

Con el Método 2, puedes estandarizar fácilmente el proceso de análisis en todos tus repositorios. Simplemente agrega el archivo analizar-repo.sh y modifica el package.json en cada uno.